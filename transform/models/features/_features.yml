version: 2

models:
  - name: feat_player_fixture_spine
    description: >
      Every player who COULD have played, crossed with the fixtures they
      could have played in. One row per player per league fixture their club
      contested in that gameweek.

      This exists because fct_player_gw is appearance-driven: a player left
      out of the squad has no row there, so non-appearance is a missing row
      rather than a zero. Non-appearance is the majority of the signal for
      P(play), so training on the fact alone means training only on players
      who played — which is the outcome being predicted.

      Squad membership comes from fct_player_snapshot, making the roster
      point-in-time correct by construction: a player signed after the
      deadline is not here, and neither is one whose club had no fixture.

      Rows exist only where a usable deadline snapshot does. Captures begin
      partway through coverage, so 2024/25 is absent entirely. That is the
      binding constraint on training set size, not a filter to be relaxed.
    tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns: [snapshot_id, player_id, match_id]
    columns:
      - name: snapshot_id
        description: The deadline this row's features are known as at.
        tests: [not_null]
      - name: player_id
        tests: [not_null]
      - name: match_id
        description: >
          The fixture being predicted. A double gameweek gives a player two
          rows here and both are legitimate targets.
        tests:
          - not_null
          - relationships:
              arguments:
                to: ref('stg_matches')
                field: match_id
      - name: team_code
        description: >
          The player's club as at the deadline, from the capture rather than
          from stg_players — so mid-season transfers resolve correctly here
          even though they do not in fct_player_gw.
        tests: [not_null]
      - name: opponent_code
        description: Always populated — league fixtures only.
        tests: [not_null]
      - name: is_home
        tests: [not_null]
      - name: fixtures_in_gw
        description: >
          League fixtures this player's club plays in this gameweek. 2 marks
          a double.
        tests:
          - dbt_utils.accepted_range:
              arguments:
                min_value: 1
                max_value: 3
      - name: did_appear
        description: >
          THE PRIMARY LABEL. False where the player was in the squad at the
          deadline but has no row in fct_player_gw for this fixture.
        tests: [not_null]
      - name: minutes
        description: Zero rather than NULL where the player did not appear.
        tests:
          - not_null
          - dbt_utils.accepted_range:
              arguments:
                min_value: 0
                max_value: 120
      - name: played_60
        description: >
          The appearance-points threshold, and the boundary that matters for
          clean sheets.
        tests: [not_null]
      - name: gw_points
        description: >
          Gameweek-level, so NULL on the second fixture of a double. Inherits
          the allocation limit documented in ADR 0005.

  - name: feat_player_form
    description: >
      A player's recent league output, as at each deadline. One row per
      player per gameweek — NOT per fixture. A double gameweek fans this out
      across both fixtures when joined to the spine, which is correct: a
      manager knows the same thing about a player before both matches.

      THE BOUNDARY IS THE DEADLINE, NOT THE KICKOFF. Bounding windows on the
      target fixture's kickoff would look correct and leak — a Thursday
      European tie falls after the deadline but before Sunday's league match.
      That is the most likely leak in this layer and the reason no window
      here uses a rows-preceding frame.

      LEAGUE ONLY. Champions League xG against a weaker opponent is not
      comparable to Premier League xG. All-competition minutes are fatigue,
      not form, and live in feat_player_load.

      Rates are per 90 and shrunk toward a positional prior with a weight of
      270 minutes, so a 20-minute cameo cannot produce a 13.5 goals-per-90
      striker. Both shrunk and unshrunk versions are carried; the difference
      between them is itself informative.
    tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns: [snapshot_id, player_id]
    columns:
      - name: snapshot_id
        tests: [not_null]
      - name: player_id
        tests: [not_null]
      - name: last_appearance_at
        description: >
          Kickoff of the most recent league appearance before this deadline.
          NULL for a cold start. The point-in-time guarantee made checkable —
          see tests/no_post_deadline_leakage.sql.
      - name: days_since_last_appearance
        description: >
          Staleness of the form window. A ten-fixture window can reach back
          two months for a returning player; this is what lets the model
          discount a stale rate rather than SQL deciding for it.
      - name: is_cold_start
        description: >
          No league appearances at all as at this deadline — a new signing, a
          promoted club's squad player, or GW1. An explicit flag rather than
          leaving the model to infer it from a wall of nulls.
        tests: [not_null]
      - name: minutes_5
        description: League minutes across the last 5 appearances.
        tests:
          - dbt_utils.accepted_range:
              arguments:
                min_value: 0
                max_value: 600
                config:
                  where: "minutes_5 is not null"
      - name: xgi_per_90_shrunk_5
        description: >
          Expected goal involvements per 90, blended toward the positional
          prior. The primary attacking-rate feature.
        tests:
          - dbt_utils.accepted_range:
              arguments:
                min_value: 0
                max_value: 3
                config:
                  where: "xgi_per_90_shrunk_5 is not null"
                  severity: warn
      - name: prior_xgi_per_90
        description: >
          League-wide per-90 rate for this position as at this deadline. The
          fallback for a cold-start row. Noisy in the opening gameweeks,
          where it is computed off one or two rounds of fixtures.

  - name: feat_player_load
    description: >
      Selection share and fatigue, as at each deadline. One row per player per
      gameweek.

      TWO BLOCKS, TWO DIFFERENT QUESTIONS. The selection block asks whether a
      player is being picked and indexes windows by the CLUB'S FIXTURES; the
      fatigue block asks whether they are tired and indexes by DAYS. A player
      who started the last three matches their club played is a starter
      whether that took ten days or thirty, but three matches in seven days is
      a different physical state from three in twenty-one.

      WHY THE SPLIT EXISTS. An earlier version used day windows for both, and
      failed at GW32 2025/26: the deadline fell three weeks after GW31 across
      the March international break, so minutes_14d was 0.00 for every player
      in the gameweek. The model's top features all read "nobody has played in
      a month" and it predicted nobody would start — p_60 peaked at 0.36
      across 841 rows where the true rate was 0.25. That inference was correct
      given what it saw; the features were wrong. A zero in a day-indexed
      window conflates "not selected" with "the league paused", and there are
      three international breaks a season.

      NULL, NOT ZERO, WHERE THE DENOMINATOR IS EMPTY. Every share here is null
      when the club played no qualifying fixtures in the window. An unknown
      share is not a share of zero, and LightGBM splits on missingness
      natively.

      ALL COMPETITIONS in both blocks, friendlies excluded from both. A midweek
      European tie is the load that causes Sunday rotation, which is why
      fct_player_gw was built at fixture grain across every competition.
    tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns: [snapshot_id, player_id]
    columns:
      - name: snapshot_id
        tests: [not_null]
      - name: player_id
        tests: [not_null]

      # -- selection block ------------------------------------------------
      - name: club_fixtures_3
        description: >
          Fixtures the club played in the window. Usually 3, fewer early in a
          season — carried rather than assumed, because it is the denominator.
      - name: minutes_share_3
        description: >
          Share of available minutes across the club's last 3 fixtures.
          Invariant to calendar gaps by construction: an international break
          changes when those fixtures happened, not which they were.
        tests:
          - dbt_utils.accepted_range:
              arguments:
                min_value: 0
                max_value: 1
                config:
                  where: "minutes_share_3 is not null"
      - name: start_rate_5
        description: >
          Fraction of the club's last 5 fixtures the player started, meaning
          60+ minutes. The primary selection feature.
        tests:
          - dbt_utils.accepted_range:
              arguments:
                min_value: 0
                max_value: 1
                config:
                  where: "start_rate_5 is not null"
      - name: minutes_share_10
        description: >
          The long-run version. Slower to react to a change in standing, so
          useful alongside the 3-fixture window rather than instead of it.
        tests:
          - dbt_utils.accepted_range:
              arguments:
                min_value: 0
                max_value: 1
                config:
                  where: "minutes_share_10 is not null"
      - name: minutes_last_club_fixture
        description: >
          What the player did in the club's most recent match. The single
          strongest selection signal — did they play last time out.
      - name: started_last_club_fixture
        tests: [not_null]
      - name: last_club_fixture_non_league
        description: >
          The club's most recent fixture was European or cup. The rotation
          signal in its rawest form: a Thursday tie predicts a Sunday rest.
        tests: [not_null]

      # -- fatigue block --------------------------------------------------
      - name: club_fixtures_14d
        description: >
          Fixtures the club played in 14 days. Zero here is what makes a zero
          minutes_14d interpretable — without it, "played 0 minutes" is
          ambiguous between rested and unavailable.
        tests: [not_null]
      - name: minutes_14d
        description: >
          Raw minutes in the window. Zero rather than NULL, because the
          denominator alongside it says whether that zero means anything.
        tests:
          - not_null
          - dbt_utils.accepted_range:
              arguments:
                min_value: 0
                max_value: 720
      - name: minutes_share_14d
        description: >
          NULL — not zero — where the club played nothing in 14 days, which is
          exactly what an international break produces.
        tests:
          - dbt_utils.accepted_range:
              arguments:
                min_value: 0
                max_value: 1
                config:
                  where: "minutes_share_14d is not null"
      - name: non_league_minutes_14d
        description: >
          European and cup minutes. League minutes in the same window predict
          selection; these predict rest.
        tests: [not_null]
      - name: acute_chronic_ratio
        description: >
          Recent load against the established baseline — the 7-day rate scaled
          to 28 days, over actual 28-day load. Above 1 means a spike. NULL
          where there is no baseline to compare against.
        tests:
          - dbt_utils.accepted_range:
              arguments:
                min_value: 0
                max_value: 4
                config:
                  where: "acute_chronic_ratio is not null"
                  severity: warn

      # -- calendar context -----------------------------------------------
      - name: last_appearance_at
        description: >
          Kickoff of the player's most recent appearance in any competition.
          Asserted below deadline_utc by the leakage test.
      - name: club_last_fixture_at
        description: >
          Kickoff of the club's most recent fixture. The denominator's anchor,
          and asserted below the deadline for the same reason.
      - name: excess_days_since_appearance
        description: >
          The player's gap since appearing, minus the club's gap since
          playing. Near zero means they featured in the last available
          fixture; large means the club has played since they last did.

          This is the feature that separates a benching from a league-wide
          pause. During a break every player's raw gap is large, but this
          stays near zero for everyone who played the last fixture before it.
      - name: follows_break
        description: >
          The club's last fixture was more than 14 days before this deadline.
          Three occur a season, so the model has few examples and benefits
          from being told rather than inferring it from a continuous variable.
        tests: [not_null]

  - name: feat_training_set
    description: >
      The assembled matrix. One row per player per fixture they could have
      played in, with every feature and every label.

      FEATURES ARE LEFT OF THE DEADLINE, LABELS RIGHT OF IT. Every feature
      column traces back to fct_player_snapshot or to a window bounded on
      deadline_utc. Nothing reads stg_player_gameweek_stats except as a
      label. That property is the only one about this model that matters, and
      tests/no_post_deadline_leakage.sql is what asserts it.

      Opponent strength joins on the OPPONENT's team_code. Inverting that
      would silently reverse every fixture-difficulty feature while producing
      no error, which is why the join is spelled out rather than aliased
      loosely.

      Expect roughly 20k rows per season with did_appear true on 40-50%. A
      much higher appearance rate means the spine is dropping
      non-appearances; much lower means it is including players who were
      never plausible selections.
    tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns: [snapshot_id, player_id, match_id]
    columns:
      - name: snapshot_id
        tests: [not_null]
      - name: player_id
        tests: [not_null]
      - name: match_id
        tests: [not_null]
      - name: position
        description: >
          Determines scoring for every component — goal value, clean sheet
          eligibility, defensive contribution threshold. Rows with a null
          position cannot be scored and should be excluded from training.
        tests:
          - accepted_values:
              arguments:
                values: ['GKP', 'DEF', 'MID', 'FWD']
      - name: opponent_elo
        description: >
          From feat_team_strength joined on opponent_code. If this correlates
          positively with the player's own returns, the join has been
          inverted.
      - name: ep_next
        description: >
          FPL's own projection, captured at the same instant as the features.
          The baseline to beat — but flat before a season starts, so rank
          correlation against it is meaningless until around GW4.
      - name: minutes_band
        description: >
          THE PRIMARY TARGET. 0 = did not play, 1 = 1-59 minutes, 2 = 60+.
          Ordinal, and the three bands are the ones the scoring rules care
          about.
        tests:
          - not_null
          - accepted_values:
              arguments:
                values: [0, 1, 2]
      - name: did_appear
        tests: [not_null]
      - name: gw_points
        description: >
          Gameweek-level label. NULL on the second fixture of a double, so
          any model trained on it must filter to primary rows.
      - name: gw_bps
        description: >
          The bonus model's target. Deterministic given recorded stats, so
          more predictable than most components — but gameweek-level, so
          train on single-fixture gameweeks only.
  
  - name: feat_player_fixture_spine_historical
    description: >
      The same shape as feat_player_fixture_spine, reconstructed for seasons
      that predate our own captures.

      The live spine takes its roster from fct_player_snapshot, which exists
      only where a deadline capture does. Captures begin in 2026/27, so
      2025/26 — the only season with completed fixtures, and therefore the
      only source of labels — produces no rows there. Without this model
      there is no supervised training data at all.

      THE ROSTER IS THE SEASON SQUAD, NOT THE GAMEWEEK SQUAD. stg_players
      carries one row per player per season, so a January signing appears
      from GW1 and a January departure appears through GW38. Bounding the
      roster by first and last observed appearance would remove them, but
      that decides availability using information from after the deadline —
      precisely the leak this layer exists to prevent. The rows stay and
      prior_appearances on feat_player_gameweek_spine carries the signal
      instead.

      Consequence: the unconditional appearance rate here sits far below the
      live spine's 30-60% band. Filter on had_appeared_before before reading
      anything into it.
    tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns: [snapshot_id, player_id, match_id]
    columns:
      - name: snapshot_id
        description: >
          Constructed to match fct_deadline_snapshot's format, so both spines
          share a key space and can be unioned without translating identifiers.
        tests: [not_null]
      - name: player_id
        tests: [not_null]
      - name: match_id
        tests:
          - not_null
          - relationships:
              arguments:
                to: ref('stg_matches')
                field: match_id
      - name: team_code
        description: >
          From stg_players, so this is the club recorded for the season rather
          than as at the deadline. A mid-season transfer is misattributed here
          in a way it is not on the live path.
        tests: [not_null]
      - name: is_reconstructed
        description: >
          Always true. Present so a union with the live spine keeps the
          distinction visible.
        tests: [not_null]
      - name: did_appear
        tests: [not_null]
      - name: minutes
        tests:
          - not_null
          - dbt_utils.accepted_range:
              arguments:
                min_value: 0
                max_value: 120

  - name: feat_player_gameweek_spine
    description: >
      One row per player per gameweek, across both the live and reconstructed
      paths. The single roster the window models join to.

      feat_player_form, feat_player_load and feat_team_strength are grained
      per gameweek and all need to know which players existed at which
      deadline. Reading fct_player_snapshot directly means they produce
      nothing for seasons predating captures, leaving the historical spine's
      labelled rows with no features attached.

      The fixture-grain spines collapse to this grain because a double
      gameweek does not change what a manager knew at the deadline. Fanning
      back out to fixture grain happens in feat_training_set.

      NO CAPTURE-SOURCED STATE. Price, status, news, chance_of_playing_next
      and ep_next exist on the live path only. Emitting them as null here
      would blur "this feature does not exist for this season" into "this
      player had no news" — two different things a model cannot separate once
      both are null. They are joined in at feat_training_set instead, where
      their absence lines up with a season boundary a reader can see.
    tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns: [snapshot_id, player_id]
    columns:
      - name: snapshot_id
        tests: [not_null]
      - name: player_id
        tests: [not_null]
      - name: position
        description: >
          Determines every scoring rule. Rows with a null position cannot be
          scored and are excluded upstream on the reconstructed path.
        tests:
          - accepted_values:
              arguments:
                values: ['GKP', 'DEF', 'MID', 'FWD']
      - name: prior_appearances
        description: >
          League appearances strictly before this deadline, counted distinct on
          match_id. Zero is meaningful rather than missing: a player with no
          prior appearance is usually not yet signed, not registered, or not in
          the manager's plans, and their appearance rate is roughly an order of
          magnitude lower than an established starter's.
        tests:
          - not_null
          - dbt_utils.accepted_range:
              arguments:
                min_value: 0
                max_value: 38
      - name: had_appeared_before
        description: >
          prior_appearances > 0. The flag that makes the reconstructed path's
          inflated negative class interpretable.
        tests: [not_null]
      - name: last_appearance_at
        description: >
          Kickoff of the most recent prior league appearance. NULL where there
          is none. Asserted below deadline_utc by the leakage test.
      - name: is_reconstructed
        description: >
          True where the row was rebuilt from published deadlines rather than
          from a capture. Capture-sourced features are structurally
          unavailable for these rows, not merely null — any model training
          across both paths must carry this or drop those columns.
        tests: [not_null]
  - name: feat_team_strength
    description: >
      Attacking and defensive strength, as at each deadline. One row per club
      per gameweek, for every club in the season — not only those with a
      fixture.

      ELO IS THE BACKBONE. fct_team_fixture carries the rating AT KICKOFF, so
      it is point-in-time by construction and is the only feature here free of
      the leakage question entirely. It also solves the promoted-club problem:
      a newly promoted side has no Premier League history but does have an
      Elo, so it starts from a defensible prior rather than a null.

      LEAGUE ONLY, for the same reason feat_player_form is. A Champions League
      goal against a weaker opponent is not evidence of Premier League
      attacking strength. All-competition load lives in feat_player_load.

      xG is summed from player rows, since fct_team_fixture carries goals but
      not xG. A team's xG is the sum of its players', which holds because
      fct_player_gw is at fixture grain. It inherits that model's end-of-season
      club assignment for mid-season transfers — immaterial at team aggregate
      level, since the player's xG lands at one of the two clubs in the
      fixture either way.

      UNDATED FIXTURES ARE EXCLUDED FROM THE WINDOWS. Nulls are peers in a
      RANGE frame, so an undated fixture falls inside every other fixture's
      window. 2025/26 GW34-38 have no kickoff time. Their goals are still real
      labels — see feat_team_fixture_spine, which keeps them — but they cannot
      contribute to a rolling rate.
    tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns: [snapshot_id, team_code]
    columns:
      - name: snapshot_id
        description: >
          The deadline these rates are known as at. Taken from
          feat_player_gameweek_spine rather than fct_deadline_snapshot, so the
          key space spans both the live and reconstructed paths.
        tests: [not_null]
      - name: team_code
        description: >
          Stable across seasons, unlike team_id. Every club in the season
          appears at every deadline, via dim_team.
        tests: [not_null]
      - name: elo_current
        description: >
          Elo at the club's most recent league fixture before this deadline.
          NULL only at a genuine cold start — GW1, before any league fixture
          has been played.
        tests:
          - dbt_utils.accepted_range:
              arguments:
                min_value: 1200
                max_value: 2200
                config:
                  where: "elo_current is not null"
      - name: xg_against_per_match_5
        description: >
          The primary defensive-strength feature, and the one the team
          defence model leans on hardest after Elo. xG rather than goals
          because the sample is five matches and goals at that sample size
          are mostly noise.
        tests:
          - dbt_utils.accepted_range:
              arguments:
                min_value: 0
                max_value: 5
                config:
                  where: "xg_against_per_match_5 is not null"
                  severity: warn
      - name: xg_for_per_match_5
        description: >
          The opponent side of the same coin. Joined as the attack a club is
          about to face.
        tests:
          - dbt_utils.accepted_range:
              arguments:
                min_value: 0
                max_value: 5
                config:
                  where: "xg_for_per_match_5 is not null"
                  severity: warn
      - name: clean_sheet_rate_10
        description: >
          Observed clean sheet rate over the last 10 league fixtures. Carried
          as the naive baseline for the team defence model to beat, not as a
          feature it should lean on — 10 matches gives a standard error of
          roughly 0.14 on a rate near the league average of 0.28.
        tests:
          - dbt_utils.accepted_range:
              arguments:
                min_value: 0
                max_value: 1
                config:
                  where: "clean_sheet_rate_10 is not null"
      - name: matches_season
        description: >
          League fixtures played before this deadline. The denominator behind
          every rate here, and what is_cold_start is derived from.
      - name: is_cold_start
        description: >
          No league fixtures at all as at this deadline. Every rolling rate is
          NULL on these rows and only Elo and the FPL strength ratings carry
          signal. An explicit flag rather than leaving a model to infer it
          from a wall of nulls.
        tests: [not_null]
      - name: strength_defence_home
        description: >
          FPL's own rating, carried for comparison against ep_next rather than
          as a modelling input. Crude next to Elo and updated infrequently.

  - name: feat_team_fixture_spine
    description: >
      Every league fixture a club could play, with the strength features known
      at that gameweek's deadline and the conceded label where one exists. Two
      rows per fixture, one per club.

      THE PREDICTION MATRIX AND THE SOURCE OF THE TRAINING SET. Unplayed
      fixtures stay, with goals_against NULL and is_played false. Filtering to
      played rows here would force a second near-identical model to score
      against, with the feature logic duplicated across both — the drift risk
      that keeps feat_player_fixture_spine and feat_training_set separate.

      ONE SPINE, NOT TWO. The player layer needs a live and a reconstructed
      path because rosters come from captures. Nothing here does: the fixture
      list and Elo come from fct_team_fixture, the rolling rates from
      feat_team_strength, and both span every season. Deadlines come from
      feat_player_gameweek_spine rather than fct_deadline_snapshot — the
      latter has no rows before captures begin, which would leave this model
      empty of labels in the only season that has them.

      UNDATED FIXTURES ARE KEPT. 2025/26 GW34-38 have no kickoff time, so
      feat_team_strength excludes them from its windows. Their gameweek is
      known, so they still join to a deadline and their goals are still real
      labels — roughly 100 rows of about 760 in the only labelled season, too
      many to discard. Congestion is NULL for them and has_congestion says so.

      This is the smallest matrix in the project by two orders of magnitude:
      about 760 rows per completed season. That constraint is the reason the
      defence model is a regularised GLM rather than a GBM.
    tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns: [snapshot_id, team_code, match_id]
    columns:
      - name: snapshot_id
        tests: [not_null]
      - name: team_code
        description: The club conceding. Features prefixed own_ describe it.
        tests: [not_null]
      - name: opponent_code
        description: >
          The club attacking. Features prefixed opp_ describe it, joined to
          feat_team_strength on this column. If opp_xg_for_5 correlates
          negatively with goals_against, the two joins have been swapped —
          an inversion that produces no error.
        tests: [not_null]
      - name: match_id
        tests:
          - not_null
          - relationships:
              arguments:
                to: ref('stg_matches')
                field: match_id
      - name: is_home
        tests: [not_null]
      - name: elo_diff
        description: >
          Own Elo minus opponent's, at kickoff. Positive means the stronger
          side, so it should correlate NEGATIVELY with goals_against.
      - name: is_played
        description: >
          Derived from result rather than from goals_against, matching
          feat_team_strength. Both are proxies for a status field the source
          does not provide, but result's nullability is one documented
          decision in fct_team_fixture rather than an incidental property of
          a label column. Every label below is NULL where this is false.
        tests: [not_null]
      - name: goals_against
        description: >
          THE PRIMARY LABEL. The team defence model fits a rate to this, and
          every defensive component falls out of the resulting distribution:
          P(0) is the clean sheet, E[floor(k/2)] the concession penalty.
        tests:
          - dbt_utils.accepted_range:
              arguments:
                min_value: 0
                max_value: 10
                config:
                  where: "is_played"
      - name: is_clean_sheet
        description: >
          goals_against = 0. NULL on unplayed fixtures. Expect roughly 0.28
          across a season; a materially different rate means the league
          filter or the home/away unpivot has gone wrong.
      - name: team_saves
        description: >
          Summed across all players rather than filtered to keepers, so an
          outfield player credited with a save still counts. The saves model's
          target.
        tests:
          - dbt_utils.accepted_range:
              arguments:
                min_value: 0
                max_value: 20
                config:
                  where: "team_saves is not null"
      - name: shots_on_target_faced
        description: >
          The opponent's shots on target in this fixture, summed from player
          rows. The volume driver behind saves, and the denominator that
          separates a busy keeper from a good one.
      - name: has_congestion
        description: >
          False where the fixture has no kickoff time, making matches_prior_14d
          and days_rest NULL. Zero rest days and unknown rest days are opposite
          signals, so these are emitted NULL with a flag rather than coalesced.
          Any model consuming them must impute explicitly and carry this
          alongside.
        tests: [not_null]
      - name: own_is_cold_start
        description: >
          Inherited from feat_team_strength. True at GW1, where only Elo
          carries signal. These rows are scored but not trained on.
        tests: [not_null]

  - name: feat_team_defence_training_set
    description: >
      The labelled subset of feat_team_fixture_spine, materialised as a view.

      Deliberately thin. Every feature is defined once, in the spine, and this
      model's only job is to make "labelled" a visible subset rather than a
      coincidence of nullability. Predictions score against the spine, training
      reads this, and neither can drift because there is nothing here to drift.

      THE ONE ADDED FILTER beyond is_played is the cold-start exclusion. A club
      at its first deadline of a season has no rolling xG, so every rate
      feature is null and the row contributes an imputation artefact rather
      than a signal. Elo still covers those fixtures at prediction time.

      Expect roughly 700 rows per completed season after both filters. A count
      materially above 760 means the unpivot is duplicating; well below 700
      means the cold-start filter is catching more than the opening gameweek.
    tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns: [snapshot_id, team_code, match_id]
    columns:
      - name: snapshot_id
        tests: [not_null]
      - name: team_code
        tests: [not_null]
      - name: match_id
        tests: [not_null]
      - name: goals_against
        description: >
          Never NULL here, unlike on the spine. That difference is the whole
          point of the model.
        tests:
          - not_null
          - dbt_utils.accepted_range:
              arguments:
                min_value: 0
                max_value: 10
      - name: is_clean_sheet
        tests: [not_null]
      - name: is_played
        description: Always true. Retained so the spine and this share a shape.
        tests: [not_null]